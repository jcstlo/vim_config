The "nvim/" directory should be placed in ~/.config on Linux systems.

Working with neovim 0.12.5

Run `:checkhealth` to confirm that everything is installed properly.

Dependencies:

- luarocks
- ripgrep
- sharkdp/fd
